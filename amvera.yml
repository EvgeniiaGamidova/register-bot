meta:
  environment: python
  toolchain:
    name: pip
    version: 3.14.2
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  persistenceMount: /data
  command: "python3 main.py"
  containerPort: 80
  servicePort: 80
